# Fixtures

**Synthetic data only.** Never commit real financial data — see [CONTRIBUTING.md](../../CONTRIBUTING.md).

| Fixture | Describes |
| --- | --- |
| `sample-statement.csv` | A 12-month statement for a fictional catering business: growing but concentrated revenue, a thin cash buffer, fixed payroll and rent, and two returned-payment events. Deliberately shaped to score in the **Watch** band. |

Planned additions: no-balance-column variant, single-signed-amount variant, `MM/DD` date variant,
seasonal-tourism revenue profile, and an adversarial set (malformed rows, hostile headers, injection
attempts in descriptions).
